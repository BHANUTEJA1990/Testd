import React from "react";
import { Background } from "reactflow";

const Header = () => {
    return (
        <div className="ui fixed menu">
            <h2>Create Component Test</h2>
        </div >
    );
};

export default Header;